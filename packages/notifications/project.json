{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "notifications",
  "projectType": "library",
  "sourceRoot": "packages/notifications/src",
  "tags": [],
  "targets": {
    "build": {
      "dependsOn": ["prepare-build-package", "^build"],
      "inputs": ["production", "^production", "{projectRoot}/*.md"],
      "outputs": ["{workspaceRoot}/dist/packages/notifications"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/notifications/**/*.[jt]s?(x)"],
        "maxWarnings": 0
      },
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "configFile": "packages/notifications/vitest.config.ts"
      }
    },
    "prepare-build-package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "env -u NO_COLOR tsx scripts/prepareTsgoPackage.mts packages/notifications"
      }
    }
  }
}
