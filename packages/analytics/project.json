{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "analytics",
  "projectType": "library",
  "sourceRoot": "packages/analytics/src",
  "tags": [],
  "targets": {
    "build": {
      "dependsOn": ["prepare-build-package", "^build"],
      "inputs": ["production", "^production", "{projectRoot}/*.md"],
      "outputs": ["{workspaceRoot}/dist/packages/analytics"]
    },
    "prepare-build-package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "env -u NO_COLOR tsx scripts/prepareTsgoPackage.mts packages/analytics"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/analytics/**/*.[jt]s?(x)"],
        "maxWarnings": 0
      },
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "configFile": "packages/analytics/vitest.config.ts"
      }
    }
  }
}
