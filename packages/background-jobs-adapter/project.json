{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "background-jobs-adapter",
  "projectType": "library",
  "sourceRoot": "packages/background-jobs-adapter/src",
  "tags": [],
  "targets": {
    "build": {
      "dependsOn": ["prepare-build-package", "^build"],
      "inputs": ["packageBuild", "^production"],
      "outputs": ["{workspaceRoot}/dist/packages/background-jobs-adapter"],
      "options": {
        "forwardAllArgs": false
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint \"packages/background-jobs-adapter/**/*.[jt]s?(x)\" --max-warnings 0"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "configFile": "packages/background-jobs-adapter/vitest.config.ts"
      }
    },
    "prepare-build-package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "env -u NO_COLOR tsx scripts/prepareTsgoPackage.mts packages/background-jobs-adapter"
      }
    }
  }
}
